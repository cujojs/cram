using douglas croskford's json2


collector process:
1) load file as text
2) pass into parser, get back list of the module ids of dependencies
3) for each dep:
	a) if module
		i) resolve to abs module id and then url
		ii) load file as text and recurse at step 1
	b) if plugin/resource
		i) load plugin module as text and recurse at step 1
		ii) look up plugin's special parser, if any
		iii) recurse at step 2

